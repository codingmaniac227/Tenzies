import './Die.css'

export default function Die(props) {
    return (
        <>
            <button
                onClick={''}
                type="button"
            > 
                {props.value}
            </button>
        </>
    )
}